name: code_utils
description: Code utils for parse and validate.
version: 0.0.1
homepage: https://github.com/ntnyq/code_utils

environment:
  sdk: '>=3.0.3 <4.0.0'
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
